# Implementation Plan: MkulimaCollect — Phase 0 + Dairy Vertical Slice

## Overview

Phase 0 (foundation) + Phase 1 vertical slice for MkulimaCollect: the production Android-first offline-first field data collection application for MkulimaScore. Bounded to T001–T030 as authorized. All implementation goes in `apps/collect/`. The Figma prototype at the workspace root must not be modified.

## Task Dependency Graph

```json
{
  "waves": [
    { "wave": 0, "tasks": ["T001"], "description": "Initialize Expo SDK 57 app" },
    { "wave": 1, "tasks": ["T002", "T003", "T004", "T006"], "description": "Design tokens, database, API adapters, EAS config" },
    { "wave": 2, "tasks": ["T005"], "description": "Auth layer" },
    { "wave": 3, "tasks": ["T007", "T008"], "description": "Navigation skeleton, connectivity hook" },
    { "wave": 4, "tasks": ["T009", "T010"], "description": "Farmer repositories, sync engine" },
    { "wave": 5, "tasks": ["T011", "T012", "T013"], "description": "Login, Home, Farmers list" },
    { "wave": 6, "tasks": ["T014"], "description": "Consent step" },
    { "wave": 7, "tasks": ["T015", "T016", "T017"], "description": "Identity, Location, Membership" },
    { "wave": 8, "tasks": ["T018"], "description": "Farm step" },
    { "wave": 9, "tasks": ["T019", "T020", "T021"], "description": "GPS map, Enterprise selection, Schema engine" },
    { "wave": 10, "tasks": ["T022"], "description": "Dairy sector" },
    { "wave": 11, "tasks": ["T023", "T024"], "description": "Financial step, Evidence capture" },
    { "wave": 12, "tasks": ["T025"], "description": "Review step" },
    { "wave": 13, "tasks": ["T026", "T027"], "description": "Submit/success screen, Sync centre" },
    { "wave": 14, "tasks": ["T028"], "description": "Complete sync engine" },
    { "wave": 15, "tasks": ["T029", "T030"], "description": "Farmer profile, Tasks screen" }
  ]
}
```

## Tasks

### T001: Initialize Expo SDK 57 application at `apps/collect/`

Run `npx create-expo-app@latest apps/collect --template blank-typescript` then configure for production:
- `expo@57.0.9`, New Architecture enabled (`newArchEnabled: true`), TypeScript strict, scheme `mkulimacollect`
- `android.minSdkVersion: 26`, `android.targetSdkVersion: 34`, `android.allowBackup: false`
- `android.package: com.mkulimascore.collect`
- tsconfig strict with `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `@/*` path alias
- Root `app/_layout.tsx`, `app/(auth)/login.tsx`, `app/(tabs)/index.tsx` placeholders
- `apps/collect/.env.example` with all `EXPO_PUBLIC_` variables documented
- Run `npx expo-doctor@latest` and resolve all critical issues
- Verify `npx tsc --noEmit` passes
- Verify Figma prototype files (`src/`, root `package.json`, `vite.config.ts`) are completely untouched

**Acceptance criteria:** `apps/collect/package.json` exists; expo-doctor passes; tsc passes; Figma prototype untouched.

### T002: Configure NativeWind v4 and port Figma design tokens

Install NativeWind v4 + tailwindcss 3.x. Configure Metro and Babel. Create all design token constants ported exactly from the Figma prototype `src/index.css`:
- `constants/colors.ts`: brand `#1A5C35`, brand-dark `#0F3D22`, brand-mid `#246B40`, brand-light `#E8F4EC`, brand-muted `#F4F8F5`, surface `#FAFAFA`, charcoal `#1C1C1E`, charcoal-700 `#3A3A3C`, charcoal-500 `#636366`, charcoal-300 `#AEAEB2`, charcoal-100 `#E5E5EA`, charcoal-50 `#F2F2F7`, amber-field `#B45309`, amber-bg `#FEF3C7`, red-field `#B91C1C`, red-bg `#FEE2E2`
- `constants/typography.ts`: Instrument Sans (display/UI), DM Mono (data/IDs/mono)
- `constants/spacing.ts`: scale from 4px base
- `constants/theme.ts`: unified theme object
- `tailwind.config.js` with all custom colors
- `metro.config.js` with NativeWind wrapper
- `babel.config.js` with NativeWind preset
- `global.css` imported in root layout
- Load Instrument Sans and DM Mono fonts via expo-font in root layout
- Smoke test: a component with `className="bg-brand text-white"` renders brand green

**Acceptance criteria:** Design tokens match Figma prototype exactly; fonts load; NativeWind className works.

### T003: Initialize Drizzle ORM with expo-sqlite and create initial migration

Install `expo-sqlite`, `drizzle-orm`, `drizzle-kit`. Create complete schema and migration:
- `lib/db/schema.ts` with ALL tables: farmers, farmer_identities, consents, memberships, farms, farm_geometries, enterprises, sector_collection_responses, production_cycles, sales, expenses, financial_records, loans, loan_repayments, evidence, tasks, qa_reviews, audit_events, sync_outbox, server_mappings, collection_sessions, app_config
- All tables have: UUID v4 PK, timestamps (created_at, updated_at ISO 8601), local_version INTEGER, synced_at nullable
- Foreign key constraints and all indexes from design.md section C
- `lib/db/migrations/0001_initial.sql` generated by drizzle-kit
- `lib/db/database.ts`: opens expo-sqlite in WAL mode, runs migrations on init, exports typed `db` instance
- `DBProvider` component wraps root layout; initializes DB before any screen renders; shows loading state during init
- `drizzle.config.ts` at `apps/collect/` root for drizzle-kit
- Integration test: migration applied to empty DB creates all tables; farmer row inserted and retrieved with correct types

**Acceptance criteria:** All 22 tables created; migration runs without error; typed Drizzle queries work.

### T004: Create API adapter interface and LocalDevelopmentAdapter

Define complete `MkulimaScoreApi` TypeScript interface and implement all three adapters:
- `lib/api/types.ts`: all request/response types (AgentCredentials, AuthResult, BootstrapConfig, FarmerAssignment, CreateFarmerPayload, FarmerSyncResult, CreateFarmPayload, FarmSyncResult, CreateEnterprisePayload, EnterpriseSyncResult, SectorResponsePayload, EvidenceMetadata, PresignedUploadResult, SyncBatch, SyncBatchResult, TaskAssignment, DuplicateSignals, DuplicateCandidate, QaResolutionPayload)
- `lib/api/ApiClient.ts`: `MkulimaScoreApi` interface
- `lib/api/adapters/LocalDevelopmentAdapter.ts`: all methods return typed mock data; clearly marked DEV ONLY; no network calls; realistic mock data matching Figma prototype (Francis O., Kiambu Cluster 04, 183 assigned farmers, etc.)
- `lib/api/adapters/MockRemoteAdapter.ts`: configurable responses + error injection for testing; artificial latency (300-800ms); configurable response loss rate; duplicate-retry simulation
- `lib/api/adapters/ProductionApiAdapter.ts`: skeleton only — all methods throw `NotImplementedError('ProductionApiAdapter not yet configured — awaiting backend endpoint documentation')`
- Adapter factory in `lib/api/index.ts`: reads `EXPO_PUBLIC_ENVIRONMENT`; returns correct adapter; production build MUST NOT fall back to dev adapter

**Acceptance criteria:** All adapters implement interface; factory returns correct adapter per env; production adapter throws on all calls.

### T005: Create AuthProvider, SessionManager, SecureTokenStore

Implement complete authentication abstraction with development identity:
- `expo-secure-store` and `expo-local-authentication` installed
- `features/auth/SecureTokenStore.ts`: get/set/delete for access_token and refresh_token via expo-secure-store ONLY; typed; throws if attempted on non-secure storage
- `features/auth/SessionManager.ts`: `getSession()`, `setSession(AuthResult)`, `clearSession()`, `isExpiringSoon()`, `refreshIfNeeded(adapter)`, `getDeviceId()` (UUID persisted to SecureStore)
- `features/auth/AuthProvider.tsx`: React context providing `{ agent, isAuthenticated, isLoading, login, logout }`; loads cached session from SecureStore on mount synchronously (no network on cold start); dev mode agent = Francis O. / Kiambu Cluster 04 / AGT-00284
- Development login: `LocalDevelopmentAdapter.authenticate()` returns a mock AuthResult with 8-hour expiry; marked DEV ONLY in UI (small "DEV" badge on login screen only in non-production builds)
- Root `_layout.tsx`: AuthProvider wraps all navigation; unauthenticated → redirect to `/(auth)/login` synchronously
- `logout()`: clears SecureStore tokens; clears Zustand UI state; preserves SQLite farmer data
- Session timeout: idle timer (8 hours); requires PIN/biometric to resume

**Acceptance criteria:** Session survives app restart; tokens only in SecureStore; dev mode clearly marked; production builds reject dev adapter.

### T006: Configure EAS Build for Android

Create EAS Build configuration for three Android profiles:
- Install EAS CLI docs; create `eas.json` with development/preview/production profiles
- development: `developmentClient: true`, debug APK, internal distribution
- preview: release APK, internal testing
- production: AAB, store submission; `EXPO_PUBLIC_ENVIRONMENT=production`
- `android.package: com.mkulimascore.collect`, `versionCode: 1`, `version: 0.1.0`
- `apps/collect/README.md`: complete developer setup guide covering install deps, create dev build, run on Android emulator, connect Metro, run tests, build each profile
- `apps/collect/.gitignore`: node_modules/, .expo/, android/, ios/, dist/

**Acceptance criteria:** `eas.json` valid; README documents complete setup workflow; no secrets in committed files.

### T007: Create base navigation structure

Build complete Expo Router navigation skeleton — all route groups and screens (placeholders acceptable for unimplemented screens):
- `app/_layout.tsx`: GestureHandlerRootView → SafeAreaProvider → DBProvider → AuthProvider → ThemeProvider → SyncProvider (stub) → Stack
- `app/(auth)/_layout.tsx`: no tab bar; Stack navigator
- `app/(auth)/login.tsx`: placeholder
- `app/(auth)/device-setup.tsx`: placeholder
- `app/(tabs)/_layout.tsx`: bottom tab navigator with Home, Farmers, [Collect FAB center], Tasks, More tabs; tab icons as inline SVG matching Figma prototype; central FAB is brand green rounded button elevated above tab bar
- `app/(tabs)/index.tsx`, `farmers/index.tsx`, `tasks.tsx`, `more.tsx`: placeholder screens
- `app/(tabs)/farmers/[farmerId]/`: all 6 profile tab files as placeholders
- `app/collect/_layout.tsx`: wizard shell with step progress bar and "Save & exit" button; no tab bar
- `app/collect/index.tsx` through `review.tsx`: placeholders
- `app/farms/[farmId]/boundary.tsx`: placeholder
- `app/evidence/capture.tsx`: placeholder
- `app/sync/index.tsx`: placeholder
- `app/+not-found.tsx`: graceful 404 screen
- Typed routes configured; all navigation type-safe

**Acceptance criteria:** All routes navigable; tab bar renders with correct icons; FAB visible; TypeScript passes.

### T008: Implement connectivity monitoring hook

Create `useConnectivity` hook and `ConnectivityIndicator` component:
- Install `@react-native-community/netinfo` (Expo SDK 57 compatible)
- `hooks/useConnectivity.ts`: exports `{ isOnline: boolean, connectionType: string | null, lastOnlineAt: Date | null }`; updates within 2 seconds of network state change; persists lastOnlineAt to AsyncStorage for display after offline restarts
- `components/ui/ConnectivityIndicator.tsx`: amber dot + "Offline" when disconnected; green dot + "Online" when connected; NEVER red; updates reactively
- Integrated into `app/(tabs)/_layout.tsx` header area so visible on all tab screens
- `SyncProvider` stub in root layout subscribes to connectivity and will trigger SyncEngine on reconnect (wired fully in T028)

**Acceptance criteria:** Indicator updates on airplane mode toggle; amber offline; green online; never red.

### T009: Implement SQLite repositories for Farmer, FarmerIdentity, Consent, Membership

Implement typed repository functions. Every write atomic (entity + outbox in one transaction):
- `features/farmers/farmerRepository.ts`: `createFarmer`, `updateFarmer`, `getFarmerById`, `getFarmersByAgent`, `searchFarmers`, `updateFarmerStatus`, `updateCompleteness`
- `features/farmers/identityRepository.ts`: `saveIdentity`, `getIdentityByFarmer`
- `features/farmers/consentRepository.ts`: `saveConsent`, `getConsentByFarmer`
- `features/farmers/membershipRepository.ts`: `saveMembership`, `getMembershipsByFarmer`, `updateMembership`
- Every `create*` function: generates UUID v4 for entity + outbox entry_uuid + operation_uuid; writes both in `db.transaction()`; outbox entry has correct entity_type, mutation_type='CREATE', depends_on=[], state='PENDING_SYNC'
- `getFarmersByAgent` uses `idx_farmers_agent` index; tested performance
- `searchFarmers`: SQLite LIKE on name, MSID/local_uuid, location
- `features/farmers/farmerCompleteness.ts`: `calculateCompleteness(farmerId, db)` returns weighted percentage (Identity+Consent 20%, Location+Membership 10%, Farm 15%, Enterprise 20%, Production 20%, Financial 10%, Evidence 5%)
- Integration tests: createFarmer → two rows exist in same transaction; FK verified; updateFarmer increments local_version

**Acceptance criteria:** All repository functions typed; atomic writes verified by integration tests; completeness calculation correct.

### T010: Implement SyncOutbox and SyncEngine skeleton

Core sync infrastructure with MockRemoteAdapter integration:
- `features/sync/SyncOutbox.ts`: `getPendingEntries()` (returns PENDING_SYNC entries respecting depends_on order), `markSyncing(entryUuid)`, `markSynced(entryUuid, serverResponse)`, `markRetry(entryUuid, error)`, `markFailed(entryUuid)`, `getEntryByOperationUuid(operationUuid)`
- `features/sync/SyncEngine.ts`: `run(adapter)` processes outbox; state transitions from design.md; dependency order enforced; HTTP 409 = success; exponential backoff table; max 10 retries → FAILED_PERMANENTLY
- `features/sync/conflictResolver.ts`: detects conflict (server_version > local_version AND local modified after sync); writes conflict; never auto-merges financial records
- `features/sync/SyncQueue.ts`: manages in-memory queue of currently syncing items; prevents duplicate concurrent submissions
- `features/sync/useSyncStatus.ts`: reactive hook via Drizzle live queries; `{ pendingCount, syncingCount, syncedCount, retryCount, failedCount, lastSyncAt, isSyncing }`
- `features/sync/serverMappingRepository.ts`: `saveMapping(localUuid, entityType, serverId)`, `getServerId(localUuid, entityType)`, `getLocalUuid(serverId, entityType)`
- MockRemoteAdapter wired: SyncEngine calls adapter methods; adapter returns mock server IDs; realistic latency + configurable failure rate
- Integration tests: PENDING→SYNCING→SYNCED; depends_on blocking; 409 as success; 10 retries→FAILED_PERMANENTLY; MSID stored in server_mappings after farmer CREATE

**Acceptance criteria:** Full sync cycle works with MockRemoteAdapter; dependency ordering enforced; idempotency proven by tests.

### T011: Build Login screen

Native React Native Login screen faithful to Figma prototype `src/screens/LoginScreen.tsx`:
- Brand green header band with MkulimaScore/MkulimaCollect branding and field intelligence tagline
- Agent ID/email field, password/PIN field (secureTextEntry with reveal toggle), organization selector (React Native Picker or custom)
- "Sign in" button: triggers `AuthProvider.login()`; loading spinner during auth; navigates to `/(tabs)` on success
- Error state: invalid credentials → inline error below button; no full-screen error
- DEV mode: small "DEV MODE" label visible when `EXPO_PUBLIC_ENVIRONMENT !== 'production'`; pre-populated with dev credentials
- Device status card at bottom: "Device registered" / "Secure" / last sync / app version — from SQLite/SecureStore, no network
- "Device setup" link → `/(auth)/device-setup`
- Correct keyboard types, returnKeyType, autoComplete on all fields
- No PII in any log statement

**Acceptance criteria:** Matches Figma prototype visually; login works with LocalDevelopmentAdapter; dev mode labelled.

### T012: Build Home dashboard screen

Native Home screen faithful to Figma prototype `src/screens/HomeScreen.tsx`, all data from SQLite:
- Agent greeting: "Good morning/afternoon/evening, [firstName]" from session
- Agent subheading: organization + cluster from session
- Today's stats (assigned, completed, in-progress, pending): from `tasks` table filtered by agent_id and today's due_date
- Sync banner: reads `useSyncStatus`; offline → amber "Working offline — your work is saved on this device"; online with pending → count + "Sync now" button triggering SyncEngine.run(); syncing → animated progress
- Farmer onboarding summary: from `farmers` table grouped by status
- Quality queue: GPS-missing count (farms with no geometry), evidence-needed count (enterprises with no evidence), QA open count
- "Start new farmer" CTA → `app/collect/consent`
- ConnectivityIndicator in header always visible
- Zero state: all counts zero when no data; correct empty messages
- No hardcoded numbers; all from SQLite

**Acceptance criteria:** All metrics from SQLite; offline state amber; sync works; zero states correct.

### T013: Build Farmers list screen

Native Farmers list screen faithful to Figma prototype `src/screens/FarmersScreen.tsx`, SQLite-backed:
- FlashList (install `@shopify/flash-list`) rendering `FarmerCard` components
- Search: debounced (200ms) SQLite LIKE query on name, local_uuid, location
- Filter chips: All, In progress, Complete, Correction, Unsynced, Dairy, Coffee, Tea — each as a SQLite WHERE clause
- `FarmerCard`: initials avatar (brand-light bg), name (semibold), ID (mono font, charcoal-300), location with pin icon, enterprise chips, completeness bar (green≥80/amber≥50/red<50), status badge, last update date, amber dot if unsynced (synced_at IS NULL)
- Badge variants: verified, incomplete, inprogress, draft, correction, pending — matching Figma prototype colors exactly
- `CompletenessBar` component: themed with correct color thresholds
- Farmer count in header from SQLite
- Empty state when no results
- Skeleton loading while first query runs
- Tapping card navigates to `/(tabs)/farmers/[farmerId]/`

**Acceptance criteria:** FlashList renders; search works; filter works; all data from SQLite; no mock arrays in screen.

### T014: Implement consent collection step

Production consent wizard step:
- `app/collect/consent.tsx` rendered inside wizard shell from `collect/_layout.tsx`
- Plain-language explanation card (brand-muted background)
- 5 required consent items as custom `ConsentCheckbox` components — unchecked by default; brand green when checked
- Optional M-PESA authorization checkbox in separate card
- Legal disclaimer card
- All items must be checked before "Continue" is active; missing items highlighted on attempt
- `ConsentCapture` card: auto-populated date/time (device), language selector, agent GPS (calls `GpsService.getCurrentPosition()`), accuracy display, signature capture area (tappable canvas → `react-native-signature-canvas`), "Photograph signed consent" camera button
- "Farmer declines" → confirmation modal → saves `declined=true` to SQLite → exits wizard gracefully with no collection
- On "Continue": `consentRepository.saveConsent()` → SQLite transaction (consent row + outbox entry); stores all consent item states, GPS coordinates, GPS accuracy, method, language, mpesa_authorized boolean, consent schema version ('1.0.0'), capture timestamp
- `collection_sessions` updated with step='identity' after consent saved
- Autosave: step_states JSON updated to SQLite on every checkbox toggle (300ms debounce)

**Acceptance criteria:** All consent fields persisted; consent GPS stored; declined path graceful; autosave proven by kill-and-reopen.

### T015: Implement identity collection step

`app/collect/identity.tsx`:
- Full name fields: first_name, middle_name, surname, preferred_name
- ID type segmented control (National ID / Passport / Alien ID)
- ID number field: visible during entry; on "Continue" → `HashUtil.sha256(value)` → only hash + last3 stored in SQLite; raw value cleared from state
- DOB field (date picker or DD/MM/YYYY text with validation)
- Gender segmented control (Female / Male / Other / Prefer not to say)
- Primary phone, alternative phone
- Preferred language selector
- ID evidence section: "Scan front" and "Scan back" camera actions using `CameraService`; captured images shown as thumbnails with "✓ Captured" state; stored as evidence records via `evidenceRepository`
- Extracted values display (read-only rows for agent review) — no auto-OCR at this stage
- Phone number masked in display after save: last 4 digits visible `07•• ••• XXXX`
- `identityRepository.saveIdentity()` on "Continue" → atomic SQLite transaction
- Autosave step state on every field blur (300ms debounce)
- `lib/crypto/HashUtil.ts`: `sha256(input: string): string` using expo-crypto; 100% unit test coverage

**Acceptance criteria:** Raw ID number absent from SQLite after save; phone masked in display; camera captures stored as evidence.

### T016: Implement location collection step

`app/collect/location.tsx`:
- Country field (pre-filled "Kenya")
- County, sub-county: searchable select from bootstrap config (populated from `LocalDevelopmentAdapter.getBootstrapConfig()` → county list); `SearchableSelect` component
- Ward, village text fields
- Nearest centre/town text field
- GPS capture card: "Capture location" button → `GpsService.getCurrentPosition()` → shows lat/lon/accuracy/altitude
- Accuracy ≤10m: green "±Xm ✓"; 10–20m: amber warning; >20m: amber "Move to open area and try again"
- "Recapture location" after first capture
- GPS values stored on farm record (first farm for this collection session) via `farmRepository` as part of the location data
- Autosave on every field change

**Acceptance criteria:** Real GPS coordinates captured from device; accuracy indicator correct; no hardcoded coordinates.

### T017: Implement membership collection step

`app/collect/membership.tsx`:
- Cooperative/SACCO/group searchable select from bootstrap list
- Member number, branch, member-since year, active/inactive toggle
- Collection centre field
- "Add another membership" support — multiple memberships per farmer
- Offline path: duplicate check skipped gracefully; note stored
- Registry matching UI placeholder: "Possible member match" card shown if adapter returns candidates — for dev this never fires (LocalDevelopmentAdapter returns empty array)
- `membershipRepository.saveMembership()` on "Continue" → atomic transaction
- All memberships persisted; FK to farmer

**Acceptance criteria:** Multiple memberships supported; offline graceful; all fields persisted.

### T018: Implement farm collection step

`app/collect/farm.tsx`:
- Farm name, tenure segmented control (Owned/Leased/Family/Communal), reported size + unit selector (Acres/Hectares), size source (Farmer reported/Title deed/Co-op record/GPS measured) chip selector
- Irrigation boolean toggle; YES → conditional inline fields: water source, irrigation type, irrigated acreage
- Farm boundary card: "Not yet mapped" → "Map farm boundary" button → `app/farms/[farmId]/boundary.tsx`
- After boundary captured and user returns to farm step: boundary card shows "Mapped ✓" + calculated area
- `farmRepository.createFarm()` on "Continue" → atomic transaction; `size_reported_acres` and `size_reported_source` set; `size_gps_acres` NULL until boundary captured
- Conditional field logic: irrigation sub-fields hidden when irrigation=false; shown when irrigation=true
- Farm FK to farmer set correctly; farm_id stored in collection_session for use by subsequent steps

**Acceptance criteria:** Farm created with correct FK; size columns independent; conditional fields work; boundary status updates on return.

### T019: Implement GPS boundary map screen

`app/farms/[farmId]/boundary.tsx` — one of the most important UX screens:
- Install `@maplibre/maplibre-react-native` (compatible version with RN 0.86.2/Expo SDK 57); requires development build
- Map tile source configured via `EXPO_PUBLIC_MAP_TILE_SOURCE` env var; use OpenFreeMap demo tiles (`https://tiles.openfreemap.org/styles/liberty`) for development; tile source clearly isolated in a config constant so it can be swapped
- Offline tile degradation: if tiles fail to load, MapLibre shows a blank/neutral canvas; GPS capture continues unaffected; no blocking error shown to agent
- Agent current position indicator: green circle + accuracy radius; updates live via `useGps` hook
- Three capture modes:
  1. Walk boundary: "Walk boundary" button → `expo-location watchPositionAsync` at 3m threshold; records points; polygon drawn in real time as dashed green line; overlay shows point count / distance walked / current accuracy
  2. Draw on map: agent taps map to place vertices; ≥3 required; polygon rendered immediately  
  3. Capture point: single GPS centroid
- "Stop recording": closes polygon; calculates area using `AreaCalculator.polygonToAcres()` (spherical excess / shoelace formula); displays GPS area vs farmer-reported area
- Variance >15%: amber warning "Reported farm size differs significantly from GPS-measured area. Please confirm with farmer."
- "Save polygon": writes `farm_geometries` row via `boundaryRepository.saveGeometry()` → SQLite (polygon_geojson GeoJSON, calculation_method, point_count, distance_m, accuracy_meters=worst fix, area_calculated_acres, captured_at, agent_id)
- Updates `farms.size_gps_acres` in same transaction
- "Redo": clears current polygon; previous geometry in audit_events
- Success overlay on save
- `features/geo/GpsService.ts`: `getCurrentPosition()`, `watchPosition(callback)`, `stopWatching()`
- `features/geo/BoundaryService.ts`: point recording, polygon building, validation (≥3 points, area > 0)
- `features/geo/AreaCalculator.ts`: `polygonToAcres(coordinates: [number, number][]): number` using spherical excess formula
- Unit test: known polygon coordinates produce correct acreage within 1% tolerance

**Acceptance criteria:** Walk boundary records real GPS; area calculated correctly; offline capture works without tiles; geometry persisted and survives restart.

### T020: Implement enterprise selection step

`app/collect/enterprise.tsx`:
- All 14 sector tiles in a 2-column grid; each with icon emoji + label
- Multi-select: selected = brand green fill; unselected = white with charcoal border
- Selection summary card: count + names of selected sectors
- On "Continue": one `enterprises` row per selected sector created via `enterpriseRepository.createEnterprise()` + outbox entry; all in single transaction; enterprise FK to farm_id and farmer_id from collection_session
- Wizard queues a `[sector].tsx` step for each selected enterprise
- For this milestone only Dairy sector form is fully implemented; other sector steps show a "Coming soon" placeholder with the sector name

**Acceptance criteria:** Multiple enterprises created per transaction; each linked to correct farm; Dairy step rendered by schema engine.

### T021: Implement sector schema engine

The generic engine that powers all sector forms — critical architecture piece:
- `features/sector-engine/SchemaEngine.ts`: `loadSchema(sectorId: string): SectorSchema`, `evaluateConditionals(schema, values): SectorSchema` (returns schema with conditional fields filtered), `validateField(field, value): ValidationResult`, `validateSection(section, values): SectionValidationResult`, `validateSchema(schema, values): SchemaValidationResult`
- `features/sector-engine/SectorSchemaRegistry.ts`: `register(schema: SectorSchema)`, `getSchema(sectorId): SectorSchema`, `getAllSectors(): SectorSummary[]`; all 14 schemas registered at startup (14 schemas required; non-Dairy may be stubs for now but must be registered)
- `features/sector-engine/FieldRenderer.tsx`: renders any `SchemaField` type — maps field.type to the correct form component: text→`Field`, number→`NumericStepper` or numeric `Field`, boolean→`SegmentedControl`, select→`SearchableSelect`, multi_select→`MultiSelect`, stepper→`NumericStepper`, currency→`CurrencyField`, unit_number→`UnitField`, date→`DateField`, camera→`CameraAction`, gps_point→`GpsField`, document→`DocumentUpload`, repeatable_group→mapped sub-fields
- Conditional logic: field with `conditionalOn` hidden until condition met; hidden field value excluded from validation and from saved payload
- ProvenanceSelect shown below any field with `provenanceOptions`; evidence prompt shown as camera button for fields with `evidencePrompt`
- `features/sector-engine/validators.ts`: all validation functions unit tested
- `app/collect/[sector].tsx`: dynamic route that loads `SchemaEngine.loadSchema(sector)` and renders via `FieldRenderer`
- Responses saved to `sector_collection_responses` with schema_id, schema_version, payload (JSON of fieldId→value), provenance_map (JSON of fieldId→ProvenanceSource)
- Unit tests: all 14 schemas parse; conditional logic evaluates correctly; required validation; min/max enforced

**Acceptance criteria:** Dynamic rendering works for Dairy; conditional fields hide/show correctly; all 14 schemas registered without errors; no scoring weights in any schema file.

### T022: Implement Dairy sector schema and form

`features/sector-engine/schemas/dairy.schema.ts` — complete production Dairy schema:
- Sections: Herd, Milk Production, Milk Sales, Costs, Animal Health, Assets
- Herd: stepper fields for total_cattle, dairy_cattle, lactating_cows, dry_cows, heifers, calves, bulls; breed multi-select (Friesian/Ayrshire/Jersey/Zebu/Cross); years_dairy integer
- Milk Production: litres_per_day_morning (number), litres_per_day_evening (number), derived display total_litres_per_day (calculated in UI from morning+evening); avg_litres_per_lactating_cow (auto from total/lactating); production_days_per_year; seasonal_high_litres; seasonal_low_litres
- Milk Sales: buyer text, cooperative/collection_centre text, litres_delivered_per_day number, delivery_frequency select (Daily/Weekly/etc), rejected_milk_litres number, price_per_litre currency KES, payment_cycle select, payment_method select; derived monthly income display
- Costs: feed_cost_monthly, fodder_cost, concentrates_cost, vet_cost_monthly, ai_breeding_cost, labour_cost_monthly, water_cost, transport_cost — all currency KES with frequency selectors
- Animal Health: vaccinations multi_select, vet_access boolean, disease_history repeatable_group (disease, year, severity, recovery), annual_mortality_pct number
- Assets: cowshed boolean, chaff_cutter boolean, milking_equipment boolean, cooling_access boolean, feed_storage boolean
- Evidence prompts: "Capture milk delivery statement" on litres_delivered_per_day; "Capture cooperative payment statement" on price_per_litre; "Capture veterinary record" in Animal Health section; "Photograph livestock/farm" camera action in Assets
- All fields have provenance options where material
- NO scoring weights, thresholds, or model coefficients anywhere in schema

**Acceptance criteria:** Dairy form renders all sections; all fields functional; evidence prompts open camera; response saved with correct schema_id='dairy', schema_version='1.0.0'; no scoring content.

### T023: Implement financial collection step

`app/collect/financial.tsx`:
- Income section: enterprise income rows (auto-populated from enterprise records with FARMER_REPORTED tag); frequency selector (Daily/Weekly/Monthly/Per cycle/Seasonal/Annual); non-farm income field
- Savings section: type (SACCO/Bank/Group), institution, amount range, frequency; provenance select
- Loans section: "Add loan" → inline loan form (lender, type, purpose, original amount, outstanding, instalment, frequency, status); FARMER_REPORTED badge on all farmer-reported loans; multiple loans supported
- Farmer-reported loans visually flagged with amber "Farmer reported — not yet verified" text
- M-PESA section: ONLY shown if `consent.mpesa_authorized = true`; file picker for PDF; IF selected PDF is password-protected: password field with secureTextEntry; password held in state only; on upload via adapter: password cleared from state immediately after call
- M-PESA for dev: `LocalDevelopmentAdapter.submitMpesaStatement()` accepts payload; password never stored
- All records via repositories → atomic SQLite transactions

**Acceptance criteria:** Multiple loans persisted; M-PESA section conditional on consent; password cleared from state; all data from SQLite.

### T024: Implement evidence capture pipeline

Production evidence acquisition:
- `expo-camera`, `expo-image-picker`, `expo-file-system` installed via `npx expo install`
- `features/evidence/CameraService.ts`: `capturePhoto(options)`, `captureDocument()` (full-screen with framing overlay), `pickFromGallery()`, `pickDocument()` — all return `{ localUri, mimeType, fileSize, width?, height? }`
- Image compression: JPEG quality 0.8; max 2MB; if compressed size still >2MB, reduce quality iteratively; verify text legibility (min 1000px shorter axis)
- MIME validation: only image/jpeg, image/png, application/pdf; others rejected
- File size limit: 20MB max; rejection with clear message
- `lib/storage/LocalFileStore.ts`: `save(sourceUri, category): string` copies file to `FileSystem.documentDirectory + 'evidence/' + uuid + ext`; returns persistent local_uri
- `lib/crypto/HashUtil.ts`: `sha256File(localUri): Promise<string>` reads file as base64 and hashes; for images uses expo-crypto
- `features/evidence/evidenceRepository.ts`: `createEvidence(data)` → evidence row + sync outbox entry atomically; hash computed and stored
- `app/evidence/capture.tsx`: full-screen camera with framing guide overlay; review/retake; evidence category selector; links to farmer/enterprise from route params; saves evidence record
- Evidence records survive navigation and app restart (stored in SQLite + local file system)
- `features/evidence/FileUploadQueue.ts`: placeholder for upload queue (wired in T028)

**Acceptance criteria:** Camera opens; images stored in app-private directory; evidence record in SQLite with hash; survives app kill/restart; no evidence lost.

### T025: Implement collection review step

`app/collect/review.tsx`:
- Reads all collection session data from SQLite via repository hooks
- Section checklist: Consent, Identity, Location/Membership, Farm, Enterprise/Production, Financial, Evidence — each with green ✓ or amber ⚠ icon
- Warning list: plain-language descriptions of missing items (e.g. "Farm boundary not yet mapped", "No evidence captured for Dairy enterprise")
- "Edit" button next to each section → navigates to that specific wizard step; existing data pre-populated
- Profile completeness percentage in header (computed via `farmerCompleteness.calculateCompleteness()`)
- "Submit farmer profile" button: writes atomic SQLite transaction setting farmer.status='SUBMITTED'; moves all LOCAL_DRAFT outbox entries to PENDING_SYNC; navigates to success screen ONLY after transaction commits
- Submit allowed with warnings present (optional items)
- No scoring language anywhere on this screen

**Acceptance criteria:** Completeness computed from real data; warnings describe real missing items; submit transaction atomic; success screen only after commit.

### T026: Implement offline submission and success screen

`app/collect/submit-success.tsx` (or separate route):
- Three phases driven by `useSyncStatus` hook:
  1. "Saved": green checkmark; "Farmer profile saved to device"; "Waiting to sync…"; amber offline dot if offline
  2. "Syncing": spinner; "Connectivity restored"; "Uploading farmer record…"; progress indicator
  3. "Synced": solid brand green circle checkmark; "Farmer profile submitted"; farmer MSID displayed from server_mappings; "Synced successfully"
- Phase 1 renders immediately (SQLite already committed in T025 before navigation)
- Phase transitions triggered by `useSyncStatus` detecting state changes in sync_outbox
- If offline at submission: stays in Phase 1; no transition; no error
- "Continue" on Phase 3: navigates to `/(tabs)/farmers` list; new farmer visible immediately (FlashList re-queries SQLite)

**Acceptance criteria:** Phase 1 renders before any sync; phase transitions are reactive from SQLite; offline stays in Phase 1 without error; farmer appears in list after Phase 3.

### T027: Build Sync Centre screen

`app/sync/index.tsx` faithful to Figma prototype `src/screens/SyncScreen.tsx`:
- Connection status banner using `useConnectivity` (online/offline + connection type if available)
- Last sync timestamp from `app_config` key 'last_sync_at'
- Three stat pills: Waiting (pending+retry count, amber if >0), Synced (count, green), Est. size (sum of evidence file_size_bytes for LOCAL items)
- FlashList of all non-SYNCED outbox entries: farmer name (from server_mappings or collection_sessions), record type (entity_type), file size if evidence, status badge (Queued/Uploading/Retry required/Failed)
- Status badge colors match Figma prototype exactly
- RETRY items: show next_retry_at timestamp
- FAILED_PERMANENTLY items: red badge, "Contact support" guidance
- Uploading items: animated progress bar row
- "Sync now" button: disabled when offline or isSyncing; triggers `SyncEngine.run()` when tapped
- DEV mode indicator: small banner "Using MockRemoteAdapter" when EXPO_PUBLIC_ENVIRONMENT=development
- All data reactive from `useSyncStatus` and Drizzle live queries

**Acceptance criteria:** All states shown correctly; sync triggered; data reactive without full refresh; DEV mode labelled.

### T028: Complete sync engine (MSID reconciliation + evidence upload lifecycle)

Harden the sync engine with full MSID reconciliation and evidence upload:
- MSID reconciliation: after farmer CREATE syncs, server returns msid → written to server_mappings; all pending child outbox entries (farm, enterprise, production) have their payloads enriched with resolved farmer msid before sending
- `depends_on` resolution: SyncEngine checks all depends_on UUIDs are SYNCED in server_mappings before attempting; skips if any unresolved
- Evidence binary upload two-phase: (1) adapter.requestEvidenceUploadUrl() → presigned_url held in memory; (2) binary PUT to presigned_url; (3) adapter.confirmEvidenceUpload(); presigned URL cleared from memory immediately after; on failure get fresh URL on retry
- MockRemoteAdapter: `requestEvidenceUploadUrl()` returns a mock presigned URL; `confirmEvidenceUpload()` simulates success/failure based on configured failure rate; `submitSyncBatch()` handles farmer/farm/enterprise CREATE with realistic latency; returns mock MSIDs
- `SyncProvider` in root layout: subscribes to connectivity events via `useConnectivity`; calls `SyncEngine.run()` on reconnection; debounced (5 second delay after reconnect to allow batch accumulation)
- Failure engineering verification:
  - Test A: create farmer offline → force close → reopen → data present in SQLite ✓
  - Test B: start sync → mock 50% failure rate → reconnect → resume; no duplicates (idempotency keys) ✓
  - Test C: MockRemoteAdapter response loss simulation → retry → same operation_uuid → server returns 409 → client marks SYNCED ✓
  - Test D: capture evidence → kill app → reopen → evidence queued with sync_status=LOCAL ✓
  - Test E: capture boundary offline → restart → geometry in SQLite ✓
  - Test F: mock migration test (add column to schema) → draft farmer preserved ✓
- `app_config` key `last_sync_at` updated after successful sync batch

**Acceptance criteria:** All 6 failure scenarios pass; MSID reconciliation correct; presigned URLs never persisted; retry without duplicates.

### T029: Build Farmer Profile screen

`app/(tabs)/farmers/[farmerId]/` — 6-tab profile:
- Profile header: initials avatar, name, MSID or local_uuid (mono), status badge, location + enterprise chips, completeness bar — all from SQLite
- Horizontal scrollable tab bar: Overview, Farms, Enterprises, Financial, Evidence, Activity
- Overview tab: identity summary card (name, ID last3, phone masked), consent summary card (date, method, language, GPS), next best actions list (computed from missing items), "Continue collecting" → reopens wizard for this farmer
- Farms tab: FarmCard per farm (name, size reported+GPS, tenure, mapping status "Mapped ✓"/"Not mapped", enterprises); "+ Add farm" button
- Enterprises tab: EnterpriseCard per enterprise (sector, completeness bar, income summary from sales table, evidence count); "+ Add enterprise"
- Financial tab: income rows with provenance badges; savings rows; loan rows with FARMER_REPORTED amber tag
- Evidence tab: evidence items (category, date, verification_status badge); "+ Add evidence" → `app/evidence/capture.tsx`
- Activity tab: `audit_events` in reverse chronological order; timeline visual with vertical line and dots matching Figma prototype
- All tabs load from SQLite; skeleton loading; empty states
- NO numeric MkulimaScore score anywhere on this screen

**Acceptance criteria:** All 6 tabs functional; all data from SQLite; no score displayed; empty states correct.

### T030: Build Tasks screen

`app/(tabs)/tasks.tsx` faithful to Figma prototype `src/screens/TasksScreen.tsx`:
- FlashList of tasks from SQLite for current agent_id, ordered by priority then due_date
- Filter chips: All, Today, High, GPS, Evidence, Correction — each filters SQLite query
- TaskCard: type badge (charcoal-50), priority badge (red/amber/grey), offline indicator "Offline ✓", due date (red if due_date ≤ today), farmer name, village, task detail text
- "Start task" → navigates to relevant farmer or collect step (from task.entity_id)
- "Done" → sets task.status='COMPLETED' in SQLite + writes audit event; task dims (opacity 0.4) but stays in list
- "Undo" → reverts done state
- Due today count in header (red badge)
- Empty state per filter
- Tasks from MockRemoteAdapter bootstrapped on login (3-5 sample tasks seeded via `LocalDevelopmentAdapter.getTasks()`)
- New tasks appear via Drizzle live query when SyncEngine pulls them on sync

**Acceptance criteria:** All data from SQLite; filter works; done/undo works; tasks seeded from dev adapter on first login.

## Notes

### Backend Integration

All sync uses `MockRemoteAdapter` for this milestone. `ProductionApiAdapter` is a skeleton. Backend endpoint documentation required before T049.

### Development Build Required

MapLibre, expo-camera, expo-location require a native development build. Run `eas build --profile development --platform android` after T001/T006.

### Design Fidelity

Every screen task references the Figma prototype screen. The prototype runs at the workspace root Vite dev server. Keep it open as visual reference during implementation.

### IP Protection Gate

No PR touching sector schemas, completeness, or API response handling may introduce scoring weights, thresholds, or model coefficients.
