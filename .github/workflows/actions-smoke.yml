name: Actions Smoke

on:
  workflow_dispatch:

jobs:
  smoke:
    name: GitHub-hosted runner smoke
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner startup
        run: echo "GitHub Actions runner startup is available for MkulimaCollect."
